'use strict';

class LinkedList {
    
  constructor(data) {
    this.data = data;
    this.next = null;
  }
  Node newHead = new Node(data);

}

module.exports = LinkedList;

prepend(data) {
    let newNode = new Node(data)
    
}
